---
phase: 01-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - db/schema/assets.ts
  - db/schema/transactions.ts
  - db/schema/manual-valuations.ts
  - db/schema/holdings.ts
  - db/schema/goals.ts
  - db/schema/portfolio-snapshots.ts
  - db/migrations/0005_add_user_id.sql
  - lib/holdings.ts
  - lib/snapshot/writer.ts
  - db/queries/assets-with-holdings.ts
  - db/queries/assets.ts
  - db/queries/goals.ts
  - db/queries/portfolio-snapshots.ts
  - db/queries/holdings.ts
  - db/queries/transactions.ts
  - app/actions/assets.ts
  - app/actions/transactions.ts
  - app/actions/manual-valuations.ts
  - app/actions/goals.ts
  - app/api/cron/snapshot/route.ts
  - app/(app)/page.tsx
  - app/(app)/assets/page.tsx
  - app/(app)/assets/[id]/page.tsx
  - app/(app)/charts/page.tsx
  - app/(app)/goals/page.tsx
  - .env.local.example
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "사용자 A가 등록한 자산은 사용자 B에게 노출되지 않는다"
    - "신규 자산/거래/목표 INSERT에는 authenticated user_id가 저장된다"
    - "모든 SELECT 쿼리는 인증된 user_id로 필터링된다"
    - "크론 잡이 CRON_TARGET_USER_ID 대상 사용자의 스냅샷만 기록한다"
    - "기존 데이터가 손실되지 않는다 (백필 마이그레이션)"
    - "6개 테이블에 RLS가 활성화되어 있다 (심층 방어)"
  artifacts:
    - path: "db/migrations/0005_add_user_id.sql"
      provides: "user_id 컬럼 추가 + FK + 인덱스 + UNIQUE 변경 + RLS 정책 SQL"
    - path: "db/schema/assets.ts"
      provides: "userId 컬럼 포함 assets 스키마"
      contains: "userId"
    - path: "db/schema/portfolio-snapshots.ts"
      provides: "(user_id, snapshot_date) 복합 UNIQUE 반영 스키마"
    - path: "lib/holdings.ts"
      provides: "upsertHoldings(assetId, userId) 시그니처"
    - path: "lib/snapshot/writer.ts"
      provides: "writePortfolioSnapshot에 userId 파라미터 포함"
    - path: "app/actions/assets.ts"
      provides: "INSERT에 userId, UPDATE/DELETE에 userId 필터"
  key_links:
    - from: "app/actions/assets.ts"
      to: "db/schema/assets.ts"
      via: "requireUser() → user.id → db.insert(assets).values({ userId: user.id })"
      pattern: "userId.*user\\.id"
    - from: "db/queries/assets-with-holdings.ts"
      to: "db/schema/assets.ts"
      via: "getAssetsWithHoldings(userId) → where(eq(assets.userId, userId))"
      pattern: "eq\\(assets\\.userId"
    - from: "app/api/cron/snapshot/route.ts"
      to: "lib/snapshot/writer.ts"
      via: "CRON_TARGET_USER_ID → writePortfolioSnapshot({ userId })"
      pattern: "CRON_TARGET_USER_ID"
---

<objective>
Phase 1~4에서 싱글 유저 가정으로 구축된 코드베이스 전체에 멀티 유저 아키텍처를 소급 적용한다.

Purpose: D-11~D-15 결정 이행 — 6개 사용자 데이터 테이블에 user_id FK를 추가하고, 앱 레이어에서 모든 Drizzle 쿼리를 user_id로 필터링하며, RLS를 심층 방어로 활성화한다.

Output:
- db/migrations/0005_add_user_id.sql — Supabase SQL Editor에서 수동 실행하는 마이그레이션
- 6개 Drizzle 스키마 파일 — userId 컬럼 추가
- db/queries/* — userId 파라미터 추가
- lib/holdings.ts, lib/snapshot/writer.ts — userId 지원
- app/actions/* — INSERT에 userId, 쿼리에 userId 필터
- app/(app)/**/page.tsx — supabase.auth.getUser()로 userId 추출 후 쿼리에 전달
- app/api/cron/snapshot/route.ts — CRON_TARGET_USER_ID env var로 단일 유저 고정
</objective>

<execution_context>
@/Users/amiz/dev/jackpot-pb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amiz/dev/jackpot-pb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md

<!-- 핵심 설계 결정 요약 -->
<!-- D-11: 6개 테이블에 user_id UUID NOT NULL REFERENCES auth.users(id) -->
<!-- D-12: RLS 활성화 — SELECT/INSERT/UPDATE/DELETE 정책 (auth.uid()) — 심층 방어 -->
<!-- D-13: 앱 레이어에서 where(eq(table.userId, user.id)) 필터 — 1차 방어 -->
<!-- D-14: Supabase Auth 직접 활용, 별도 사용자 테이블 불필요 -->
<!-- D-15: 공개 회원가입 없음, 수동 생성 -->
<!-- price_cache: user_id 추가 안 함 — 전역 공유 테이블 -->
<!-- DATABASE_URL = postgres superuser → RLS 우회. RLS는 Dashboard/PostgREST 방어용 -->
<!-- portfolio_snapshots: snapshot_date UNIQUE → (user_id, snapshot_date) 복합 UNIQUE로 변경 -->
</context>

<interfaces>
<!-- 실행자가 참조해야 할 현재 코드 계약 -->

현재 스키마 패턴 (user_id 추가 전):
```typescript
// db/schema/assets.ts — 현재 상태 (userId 없음)
export const assets = pgTable('assets', {
  id: uuid('id').primaryKey().defaultRandom(),
  name: varchar('name', { length: 255 }).notNull(),
  // ... 기타 컬럼
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
})
```

추가 후 패턴:
```typescript
// userId: uuid('user_id').notNull() — auth.users FK 없이 uuid만 선언 (Q6 결정)
// FK는 SQL 마이그레이션에서 수동 추가
userId: uuid('user_id').notNull(),
```

현재 requireUser() 패턴 (이미 모든 actions에 존재):
```typescript
async function requireUser() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')
  return user
}
```

Server Component에서 userId 추출 패턴 (신규):
```typescript
import { createClient } from '@/utils/supabase/server'
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) redirect('/login')
// user.id를 쿼리 함수에 전달
```

현재 upsertHoldings 시그니처:
```typescript
export async function upsertHoldings(assetId: string): Promise<void>
// 변경 후: export async function upsertHoldings(assetId: string, userId: string): Promise<void>
```

현재 writePortfolioSnapshot 시그니처:
```typescript
export interface SnapshotParams {
  snapshotDate: string
  totalValueKrw: number
  totalCostKrw: number
  returnBps: number
}
// 변경 후: userId: string 필드 추가
```

portfolio_snapshots 현재 UNIQUE:
```typescript
snapshotDate: date('snapshot_date').notNull().unique()
// 변경 후: .unique() 제거, SQL에서 복합 UNIQUE 추가
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 마이그레이션 SQL + Drizzle 스키마 파일 업데이트</name>
  <files>
    db/migrations/0005_add_user_id.sql
    db/schema/assets.ts
    db/schema/transactions.ts
    db/schema/manual-valuations.ts
    db/schema/holdings.ts
    db/schema/goals.ts
    db/schema/portfolio-snapshots.ts
    .env.local.example
  </files>
  <action>
**Step A — 마이그레이션 SQL 파일 생성**

`db/migrations/0005_add_user_id.sql`을 아래 내용으로 생성한다. 이 파일은 Supabase Dashboard → SQL Editor에서 수동으로 실행한다 (drizzle-kit migrate 아님 — 기존 데이터 백필 로직이 포함되어 있어 수동 실행 필요).

```sql
-- Migration: 0005_add_user_id
-- Phase: 01-foundation Plan 04 — 멀티 유저 아키텍처
-- 실행 방법: Supabase Dashboard → SQL Editor에서 아래 SQL 전체를 붙여넣어 실행
-- 사전 조건: Supabase Dashboard → Authentication → Users에서 관리자 UUID 확인 후
--            아래 '<ADMIN_USER_UUID>' 자리를 실제 UUID로 교체
--
-- IMPORTANT: '<ADMIN_USER_UUID>'를 실제 UUID로 교체하지 않으면 실행 실패

-- ============================================================
-- 1. NULLABLE user_id 컬럼 추가 (기존 데이터가 있으므로 nullable first)
-- ============================================================
ALTER TABLE assets ADD COLUMN IF NOT EXISTS user_id uuid;
ALTER TABLE transactions ADD COLUMN IF NOT EXISTS user_id uuid;
ALTER TABLE manual_valuations ADD COLUMN IF NOT EXISTS user_id uuid;
ALTER TABLE holdings ADD COLUMN IF NOT EXISTS user_id uuid;
ALTER TABLE goals ADD COLUMN IF NOT EXISTS user_id uuid;
ALTER TABLE portfolio_snapshots ADD COLUMN IF NOT EXISTS user_id uuid;

-- ============================================================
-- 2. 기존 행 백필 — '<ADMIN_USER_UUID>'를 실제 UUID로 교체 후 실행
-- ============================================================
UPDATE assets SET user_id = '<ADMIN_USER_UUID>' WHERE user_id IS NULL;
UPDATE transactions SET user_id = '<ADMIN_USER_UUID>' WHERE user_id IS NULL;
UPDATE manual_valuations SET user_id = '<ADMIN_USER_UUID>' WHERE user_id IS NULL;
UPDATE holdings SET user_id = '<ADMIN_USER_UUID>' WHERE user_id IS NULL;
UPDATE goals SET user_id = '<ADMIN_USER_UUID>' WHERE user_id IS NULL;
UPDATE portfolio_snapshots SET user_id = '<ADMIN_USER_UUID>' WHERE user_id IS NULL;

-- ============================================================
-- 3. NOT NULL 제약 추가
-- ============================================================
ALTER TABLE assets ALTER COLUMN user_id SET NOT NULL;
ALTER TABLE transactions ALTER COLUMN user_id SET NOT NULL;
ALTER TABLE manual_valuations ALTER COLUMN user_id SET NOT NULL;
ALTER TABLE holdings ALTER COLUMN user_id SET NOT NULL;
ALTER TABLE goals ALTER COLUMN user_id SET NOT NULL;
ALTER TABLE portfolio_snapshots ALTER COLUMN user_id SET NOT NULL;

-- ============================================================
-- 4. FK 제약 추가 (auth.users 참조, ON DELETE CASCADE)
-- ============================================================
ALTER TABLE assets ADD CONSTRAINT assets_user_id_fk
  FOREIGN KEY (user_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE transactions ADD CONSTRAINT transactions_user_id_fk
  FOREIGN KEY (user_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE manual_valuations ADD CONSTRAINT manual_valuations_user_id_fk
  FOREIGN KEY (user_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE holdings ADD CONSTRAINT holdings_user_id_fk
  FOREIGN KEY (user_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE goals ADD CONSTRAINT goals_user_id_fk
  FOREIGN KEY (user_id) REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE portfolio_snapshots ADD CONSTRAINT portfolio_snapshots_user_id_fk
  FOREIGN KEY (user_id) REFERENCES auth.users(id) ON DELETE CASCADE;

-- ============================================================
-- 5. portfolio_snapshots UNIQUE 제약 변경
--    snapshot_date 단독 UNIQUE → (user_id, snapshot_date) 복합 UNIQUE
-- ============================================================
ALTER TABLE portfolio_snapshots DROP CONSTRAINT IF EXISTS portfolio_snapshots_snapshot_date_unique;
ALTER TABLE portfolio_snapshots ADD CONSTRAINT portfolio_snapshots_user_snapshot_unique
  UNIQUE (user_id, snapshot_date);

-- ============================================================
-- 6. user_id 인덱스 추가 (RLS 정책 + 쿼리 성능)
-- ============================================================
CREATE INDEX IF NOT EXISTS assets_user_id_idx ON assets(user_id);
CREATE INDEX IF NOT EXISTS transactions_user_id_idx ON transactions(user_id);
CREATE INDEX IF NOT EXISTS manual_valuations_user_id_idx ON manual_valuations(user_id);
CREATE INDEX IF NOT EXISTS holdings_user_id_idx ON holdings(user_id);
CREATE INDEX IF NOT EXISTS goals_user_id_idx ON goals(user_id);
CREATE INDEX IF NOT EXISTS portfolio_snapshots_user_id_idx ON portfolio_snapshots(user_id);

-- ============================================================
-- 7. RLS 활성화 + 정책 추가 (D-12 심층 방어)
--    Drizzle DATABASE_URL = superuser → RLS 우회하므로 실질 방어는 앱 레이어 필터
--    RLS는 Supabase Dashboard, PostgREST, anon key 노출 시 방어
--    성능: auth.uid() 대신 (select auth.uid()) 사용 (상수로 최적화됨)
-- ============================================================

-- assets
ALTER TABLE assets ENABLE ROW LEVEL SECURITY;
CREATE POLICY "assets_select_own" ON assets FOR SELECT TO authenticated
  USING ((select auth.uid()) = user_id);
CREATE POLICY "assets_insert_own" ON assets FOR INSERT TO authenticated
  WITH CHECK ((select auth.uid()) = user_id);
CREATE POLICY "assets_update_own" ON assets FOR UPDATE TO authenticated
  USING ((select auth.uid()) = user_id) WITH CHECK ((select auth.uid()) = user_id);
CREATE POLICY "assets_delete_own" ON assets FOR DELETE TO authenticated
  USING ((select auth.uid()) = user_id);

-- transactions
ALTER TABLE transactions ENABLE ROW LEVEL SECURITY;
CREATE POLICY "transactions_select_own" ON transactions FOR SELECT TO authenticated
  USING ((select auth.uid()) = user_id);
CREATE POLICY "transactions_insert_own" ON transactions FOR INSERT TO authenticated
  WITH CHECK ((select auth.uid()) = user_id);
CREATE POLICY "transactions_update_own" ON transactions FOR UPDATE TO authenticated
  USING ((select auth.uid()) = user_id) WITH CHECK ((select auth.uid()) = user_id);
CREATE POLICY "transactions_delete_own" ON transactions FOR DELETE TO authenticated
  USING ((select auth.uid()) = user_id);

-- manual_valuations (append-only: SELECT + INSERT만)
ALTER TABLE manual_valuations ENABLE ROW LEVEL SECURITY;
CREATE POLICY "manual_valuations_select_own" ON manual_valuations FOR SELECT TO authenticated
  USING ((select auth.uid()) = user_id);
CREATE POLICY "manual_valuations_insert_own" ON manual_valuations FOR INSERT TO authenticated
  WITH CHECK ((select auth.uid()) = user_id);
-- UPDATE는 cron superuser가 수행하므로 authenticated 정책 없음

-- holdings
ALTER TABLE holdings ENABLE ROW LEVEL SECURITY;
CREATE POLICY "holdings_select_own" ON holdings FOR SELECT TO authenticated
  USING ((select auth.uid()) = user_id);
CREATE POLICY "holdings_insert_own" ON holdings FOR INSERT TO authenticated
  WITH CHECK ((select auth.uid()) = user_id);
CREATE POLICY "holdings_update_own" ON holdings FOR UPDATE TO authenticated
  USING ((select auth.uid()) = user_id) WITH CHECK ((select auth.uid()) = user_id);
CREATE POLICY "holdings_delete_own" ON holdings FOR DELETE TO authenticated
  USING ((select auth.uid()) = user_id);

-- goals
ALTER TABLE goals ENABLE ROW LEVEL SECURITY;
CREATE POLICY "goals_select_own" ON goals FOR SELECT TO authenticated
  USING ((select auth.uid()) = user_id);
CREATE POLICY "goals_insert_own" ON goals FOR INSERT TO authenticated
  WITH CHECK ((select auth.uid()) = user_id);
CREATE POLICY "goals_update_own" ON goals FOR UPDATE TO authenticated
  USING ((select auth.uid()) = user_id) WITH CHECK ((select auth.uid()) = user_id);
CREATE POLICY "goals_delete_own" ON goals FOR DELETE TO authenticated
  USING ((select auth.uid()) = user_id);

-- portfolio_snapshots (cron이 INSERT하므로 INSERT 정책 없음 — superuser가 RLS 우회)
ALTER TABLE portfolio_snapshots ENABLE ROW LEVEL SECURITY;
CREATE POLICY "snapshots_select_own" ON portfolio_snapshots FOR SELECT TO authenticated
  USING ((select auth.uid()) = user_id);
```

**Step B — Drizzle 스키마 파일 업데이트**

6개 스키마 파일 각각에 `userId: uuid('user_id').notNull()` 컬럼을 추가한다. FK 참조는 Drizzle TypeScript 스키마에 포함하지 않는다 (auth.users는 Drizzle 외부 스키마). SQL 마이그레이션에서 이미 FK를 추가했다.

`db/schema/assets.ts`:
- `pgTable` import에 `uuid`가 이미 있음을 확인 (없으면 추가)
- 기존 컬럼 목록에서 `id: uuid('id').primaryKey()` 바로 다음에 추가:
  `userId: uuid('user_id').notNull(),`

`db/schema/transactions.ts`:
- `assetId: uuid('asset_id')...` 다음에:
  `userId: uuid('user_id').notNull(),`

`db/schema/manual-valuations.ts`:
- `assetId: uuid('asset_id')...` 다음에:
  `userId: uuid('user_id').notNull(),`

`db/schema/holdings.ts`:
- `assetId: uuid('asset_id')...` 다음에:
  `userId: uuid('user_id').notNull(),`

`db/schema/goals.ts`:
- `id: uuid('id').primaryKey()` 다음에:
  `userId: uuid('user_id').notNull(),`

`db/schema/portfolio-snapshots.ts`:
- `id: uuid('id').primaryKey()` 다음에:
  `userId: uuid('user_id').notNull(),`
- `snapshotDate` 컬럼에서 `.unique()` 제거 (복합 UNIQUE는 SQL에서 관리):
  `snapshotDate: date('snapshot_date').notNull(),`

**Step C — .env.local.example 업데이트**

파일 끝에 추가:
```
# 크론 잡 대상 유저 UUID (멀티 유저 환경에서 스냅샷을 기록할 단일 사용자)
# Supabase Dashboard → Authentication → Users에서 UUID 확인
# 미설정 시 크론 잡이 스냅샷을 기록하지 않음
CRON_TARGET_USER_ID=your-admin-user-uuid-here
```
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const sql = fs.readFileSync('db/migrations/0005_add_user_id.sql', 'utf8'); console.assert(sql.includes('user_id'), 'SQL: user_id 없음'); console.assert(sql.includes('ENABLE ROW LEVEL SECURITY'), 'SQL: RLS 없음'); console.assert(sql.includes('portfolio_snapshots_user_snapshot_unique'), 'SQL: 복합 UNIQUE 없음'); console.log('SQL 파일 구조 OK')"</automated>
    <automated>node -e "const fs = require('fs'); const schema = fs.readFileSync('db/schema/assets.ts', 'utf8'); console.assert(schema.includes('userId'), 'assets.ts: userId 없음'); const snap = fs.readFileSync('db/schema/portfolio-snapshots.ts', 'utf8'); console.assert(!snap.includes('.unique()') || snap.includes('composite'), 'snapshots.ts: .unique() 제거 확인'); console.log('스키마 파일 OK')"</automated>
  </verify>
  <done>
- db/migrations/0005_add_user_id.sql 생성됨 (nullable 추가 → 백필 → NOT NULL → FK → UNIQUE 변경 → 인덱스 → RLS 정책 순서)
- 6개 스키마 파일 모두 userId: uuid('user_id').notNull() 포함
- portfolio-snapshots.ts에서 .unique() 제거
- .env.local.example에 CRON_TARGET_USER_ID 문서화
  </done>
</task>

<task type="auto">
  <name>Task 2: 데이터 계층 업데이트 — lib + db/queries</name>
  <files>
    lib/holdings.ts
    lib/snapshot/writer.ts
    db/queries/assets-with-holdings.ts
    db/queries/assets.ts
    db/queries/goals.ts
    db/queries/portfolio-snapshots.ts
    db/queries/holdings.ts
    db/queries/transactions.ts
  </files>
  <action>
**lib/holdings.ts — upsertHoldings에 userId 파라미터 추가**

`upsertHoldings` 함수 시그니처를 `(assetId: string, userId: string)` 로 변경한다.
`db.insert(holdings).values(...)` 블록에 `userId` 추가:
```typescript
await db
  .insert(holdings)
  .values({
    assetId,
    userId,   // 추가
    totalQuantity: result.totalQuantity,
    avgCostPerUnit: result.avgCostPerUnit,
    totalCostKrw: result.totalCostKrw,
    updatedAt: new Date(),
  })
  .onConflictDoUpdate({
    target: holdings.assetId,
    set: {
      // userId는 업데이트 대상이 아님 — 자산 소유자는 변경되지 않음
      totalQuantity: result.totalQuantity,
      avgCostPerUnit: result.avgCostPerUnit,
      totalCostKrw: result.totalCostKrw,
      updatedAt: new Date(),
    },
  })
```

**lib/snapshot/writer.ts — SnapshotParams에 userId 추가**

```typescript
export interface SnapshotParams {
  snapshotDate: string
  totalValueKrw: number
  totalCostKrw: number
  returnBps: number
  userId: string   // 추가
}
```

`db.insert(portfolioSnapshots).values(params)` — params에 userId가 포함되므로 변경 불필요.
단, `onConflictDoNothing()` 타겟이 이제 `(user_id, snapshot_date)` 복합 UNIQUE이므로 정상 동작한다.

**db/queries/assets-with-holdings.ts — userId 필터 추가**

`getAssetsWithHoldings(userId: string)` 로 시그니처 변경.
기존 `.from(assets)` 다음에 `.where(eq(assets.userId, userId))` 추가.
수동 valuations 서브쿼리도 userId 필터 추가:
```typescript
latestManualValuationKrw: sql<number | null>`(
  SELECT mv.value_krw
  FROM manual_valuations mv
  WHERE mv.asset_id = ${assets.id}
    AND mv.user_id = ${userId}
  ORDER BY mv.valued_at DESC, mv.created_at DESC
  LIMIT 1
)`,
```

**db/queries/assets.ts — userId 필터 추가**

`getAssets(userId: string)` — `.from(assets).where(eq(assets.userId, userId))`
`getAssetById(id: string, userId: string)` — `.where(and(eq(assets.id, id), eq(assets.userId, userId)))`
`and` import 추가: `import { eq, and } from 'drizzle-orm'`

**db/queries/goals.ts — userId 필터 추가**

`listGoals(userId: string)` — `.from(goals).where(eq(goals.userId, userId))`
`getGoalById(id: string, userId: string)` — `.where(and(eq(goals.id, id), eq(goals.userId, userId)))`

**db/queries/portfolio-snapshots.ts — userId 필터 추가**

`getAllSnapshots(userId: string)` — `.from(portfolioSnapshots).where(eq(portfolioSnapshots.userId, userId))`

**db/queries/holdings.ts — userId 추가 불필요**

`getHoldingByAssetId(assetId: string)` 는 assetId로 조회. assetId가 이미 특정 사용자의 자산이므로 userId 필터 중복 불필요. 변경하지 않는다.

**db/queries/transactions.ts — userId 필터 추가**

`getTransactionsByAsset(assetId: string)` 는 assetId 기반 — 변경 불필요 (assetId는 이미 user-scoped).
`getAllTransactionsWithAsset()` 는 대시보드/거래내역 페이지에서 호출. userId 파라미터 추가:
`getAllTransactionsWithAsset(userId: string)` — `.from(transactions).innerJoin(assets, and(eq(transactions.assetId, assets.id), eq(assets.userId, userId)))`

**주의:** 모든 파일에서 `eq` 외 필요한 drizzle 함수 import 확인 (`and`, `asc`, `desc` 등).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- lib/holdings.ts: upsertHoldings(assetId, userId) 시그니처, INSERT에 userId 포함
- lib/snapshot/writer.ts: SnapshotParams에 userId 필드
- db/queries/assets-with-holdings.ts: getAssetsWithHoldings(userId) + where 필터
- db/queries/assets.ts: getAssets(userId), getAssetById(id, userId) + where 필터
- db/queries/goals.ts: listGoals(userId), getGoalById(id, userId) + where 필터
- db/queries/portfolio-snapshots.ts: getAllSnapshots(userId) + where 필터
- db/queries/transactions.ts: getAllTransactionsWithAsset(userId) + JOIN 필터
- TypeScript 컴파일 에러 없음 (tsc --noEmit 통과)
  </done>
</task>

<task type="auto">
  <name>Task 3: 앱 레이어 업데이트 — Server Actions + Cron + Server Components</name>
  <files>
    app/actions/assets.ts
    app/actions/transactions.ts
    app/actions/manual-valuations.ts
    app/actions/goals.ts
    app/api/cron/snapshot/route.ts
    app/(app)/page.tsx
    app/(app)/assets/page.tsx
    app/(app)/assets/[id]/page.tsx
    app/(app)/charts/page.tsx
    app/(app)/goals/page.tsx
  </files>
  <action>
**app/actions/assets.ts**

`requireUser()`는 이미 존재. 반환값인 `user`를 사용해 userId를 추출한다.

`createAsset`:
- `await requireUser()` → `const user = await requireUser()`
- `db.insert(assets).values({ ...rest, ... })` → `userId: user.id` 추가
- `db.insert(transactions).values({...})` → `userId: user.id` 추가
- `upsertHoldings(newAsset.id)` → `upsertHoldings(newAsset.id, user.id)`

`updateAsset(id, data)`:
- `const user = await requireUser()`
- `db.select(...).from(assets).where(eq(assets.id, id))` → `and(eq(assets.id, id), eq(assets.userId, user.id))` (타인의 자산 수정 방지)
- `db.update(assets).set({...}).where(eq(assets.id, id))` → `and(eq(assets.id, id), eq(assets.userId, user.id))`
- 신규 transaction INSERT → `userId: user.id` 추가
- `upsertHoldings(id)` → `upsertHoldings(id, user.id)`

`deleteAsset(id)`:
- `const user = await requireUser()`
- `tx.delete(assets).where(eq(assets.id, id))` → `and(eq(assets.id, id), eq(assets.userId, user.id))`
- 자식 테이블(transactions, manualValuations, holdings) DELETE는 assetId 기반이므로 변경 불필요 (assetId 소유권은 assets 삭제 시 이미 검증됨)

---

**app/actions/transactions.ts**

`createTransaction(assetId, data)`:
- `const user = await requireUser()`
- 자산 조회: `db.select({ currency: assets.currency }).from(assets).where(and(eq(assets.id, assetId), eq(assets.userId, user.id)))` — 타인의 자산에 거래 추가 방지
- `db.insert(transactions).values({...})` → `userId: user.id` 추가
- `upsertHoldings(assetId)` → `upsertHoldings(assetId, user.id)`

`deleteTransaction(transactionId, assetId)`:
- `const user = await requireUser()`
- `db.delete(transactions).where(eq(transactions.id, transactionId))` → `and(eq(transactions.id, transactionId), eq(transactions.userId, user.id))`
- `upsertHoldings(assetId)` → `upsertHoldings(assetId, user.id)`

`voidTransaction(transactionId, assetId)`:
- `const user = await requireUser()`
- `db.update(transactions).set({ isVoided: true }).where(eq(transactions.id, transactionId))` → `and(eq(transactions.id, transactionId), eq(transactions.userId, user.id))`
- `upsertHoldings(assetId)` → `upsertHoldings(assetId, user.id)`

`updateTransaction(transactionId, assetId, data)`:
- `const user = await requireUser()`
- 자산 조회 where에 `eq(assets.userId, user.id)` 추가
- UPDATE where에 `eq(transactions.userId, user.id)` 추가
- `upsertHoldings(assetId)` → `upsertHoldings(assetId, user.id)`

---

**app/actions/manual-valuations.ts**

`createManualValuation(assetId, data)`:
- `const user = await requireUser()`
- 기존 valuation 조회 `where(and(eq(manualValuations.assetId, assetId), ...))` → `and(eq(manualValuations.assetId, assetId), eq(manualValuations.userId, user.id), ...)`
- UPDATE where에도 userId 필터 (onlyOwn)
- INSERT `.values({...})` → `userId: user.id` 추가

---

**app/actions/goals.ts**

`createGoal(data)`:
- `const user = await requireUser()`
- `db.insert(goals).values({...})` → `userId: user.id` 추가

`updateGoal(id, data)`:
- `const user = await requireUser()`
- `db.update(goals).set({...}).where(eq(goals.id, id))` → `and(eq(goals.id, id), eq(goals.userId, user.id))`

`deleteGoal(id)`:
- `const user = await requireUser()`
- `db.delete(goals).where(eq(goals.id, id))` → `and(eq(goals.id, id), eq(goals.userId, user.id))`

---

**app/api/cron/snapshot/route.ts**

기존 CRON_SECRET 검증 이후, 다음 로직 추가:

```typescript
// CRON_TARGET_USER_ID: 스냅샷을 기록할 대상 유저 UUID
// .env.local (및 Vercel 환경변수)에 설정 필요
const targetUserId = process.env.CRON_TARGET_USER_ID
if (!targetUserId) {
  return Response.json({ ok: false, error: 'CRON_TARGET_USER_ID not configured' }, { status: 500 })
}
```

`getAssetsWithHoldings()` → `getAssetsWithHoldings(targetUserId)`
`writePortfolioSnapshot({ snapshotDate, totalValueKrw, totalCostKrw, returnBps })` → `{ ..., userId: targetUserId }`

또한 `liveAssets` 조회 쿼리에도 userId 필터 추가:
```typescript
const liveAssets = await db
  .select({ ticker: assets.ticker, assetType: assets.assetType })
  .from(assets)
  .where(and(eq(assets.priceType, 'live'), isNotNull(assets.ticker), eq(assets.userId, targetUserId)))
```

---

**Server Components — userId 추출 후 쿼리에 전달**

`app/(app)/page.tsx` (DashboardPage):
- 상단에 추가:
  ```typescript
  import { redirect } from 'next/navigation'
  import { createClient } from '@/utils/supabase/server'
  ```
- 함수 본문 시작:
  ```typescript
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')
  ```
- `loadPerformances()` → userId를 전달하는 방식으로 변경 (아래 참조)
- `listGoals()` → `listGoals(user.id)`

`lib/server/load-performances.ts`:
- `loadPerformances(userId: string)` 시그니처로 변경
- `getAssetsWithHoldings()` → `getAssetsWithHoldings(userId)`
- 이 파일도 files_modified에 포함 (누락 방지)

`app/(app)/assets/page.tsx`:
- supabase.auth.getUser()로 userId 추출 후 loadPerformances(user.id)에 전달

`app/(app)/assets/[id]/page.tsx`:
- supabase.auth.getUser()로 userId 추출
- `getAssetById(id)` → `getAssetById(id, user.id)`
- 자산이 없거나 소유자가 다르면 notFound()

`app/(app)/charts/page.tsx`:
- supabase.auth.getUser()로 userId 추출
- `getAllSnapshots()` → `getAllSnapshots(user.id)`
- `loadPerformances()` → `loadPerformances(user.id)`

`app/(app)/goals/page.tsx`:
- supabase.auth.getUser()로 userId 추출
- `listGoals()` → `listGoals(user.id)`
- `getAllSnapshots()` → `getAllSnapshots(user.id)`

**주의:** `createClient`는 이미 `@/utils/supabase/server`에서 export됨 (01-03에서 구현). 미들웨어가 이미 인증을 보장하므로 Server Component에서 getUser()가 null을 반환하는 경우는 비정상 상태지만, 안전을 위해 redirect('/login')를 추가한다.

**주의:** `lib/server/load-performances.ts`도 수정 필요하므로 이 Task에서 함께 처리한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -40</automated>
    <automated>grep -rn "upsertHoldings(" app/actions/ lib/ | grep -v "export\|import\|function" | grep -v "user\.id\|userId" | head -10</automated>
  </verify>
  <done>
- app/actions/assets.ts: 모든 INSERT에 userId, UPDATE/DELETE에 userId 필터
- app/actions/transactions.ts: 모든 INSERT에 userId, UPDATE/DELETE에 userId 필터, upsertHoldings 시그니처 업데이트
- app/actions/manual-valuations.ts: INSERT에 userId, 조회/업데이트 where에 userId 필터
- app/actions/goals.ts: INSERT에 userId, UPDATE/DELETE에 userId 필터
- app/api/cron/snapshot/route.ts: CRON_TARGET_USER_ID env var 검증, getAssetsWithHoldings(targetUserId), writePortfolioSnapshot에 userId 전달
- 모든 Server Component (page.tsx)에서 supabase.auth.getUser()로 userId 추출 후 쿼리 함수에 전달
- lib/server/load-performances.ts: loadPerformances(userId) 시그니처
- TypeScript 컴파일 에러 없음
- upsertHoldings 호출 모두 userId 인수 포함
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | FormData 또는 직렬화된 인수가 untrusted input으로 진입 |
| Vercel Cron → /api/cron/snapshot | GET 요청, Authorization 헤더 검증 |
| Supabase Auth → App Layer | auth.getUser() 검증 결과를 신뢰의 근거로 사용 |
| App Layer (Drizzle) → PostgreSQL | DATABASE_URL superuser 연결 — RLS 우회 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Spoofing | Server Actions (assets, goals, transactions) | mitigate | requireUser()로 Supabase auth.getUser() 검증; user.id를 모든 쿼리에 where 필터로 적용 |
| T-01-04-02 | Tampering | updateAsset / updateGoal / voidTransaction | mitigate | UPDATE WHERE에 and(eq(id, id), eq(userId, user.id)) — 타인의 레코드 수정 불가 |
| T-01-04-03 | Info Disclosure | getAssetsWithHoldings, listGoals, getAllSnapshots | mitigate | 모든 쿼리에 eq(table.userId, userId) 필터 — 타인 데이터 노출 불가 |
| T-01-04-04 | Elevation of Privilege | Cron route POST 아닌 GET | accept | CRON_SECRET 헤더 검증이 1차 방어; cron은 CRON_TARGET_USER_ID 단일 유저만 처리 |
| T-01-04-05 | Info Disclosure | Drizzle DATABASE_URL RLS bypass | accept | 설계적 결정: superuser 연결은 앱 레이어 필터가 1차 방어; RLS는 Dashboard/PostgREST 심층 방어 (D-13 이중 방어 전략) |
| T-01-04-06 | Tampering | portfolio_snapshots INSERT — cron이 다른 유저 UUID 주입 | mitigate | CRON_TARGET_USER_ID env var만 허용; 요청 body에서 userId를 받지 않음 |
</threat_model>

<verification>
마이그레이션 실행 전 체크리스트 (사람이 수행):
1. Supabase Dashboard → Authentication → Users에서 관리자 UUID 확인
2. 0005_add_user_id.sql의 `<ADMIN_USER_UUID>` 를 실제 UUID로 교체
3. Supabase Dashboard → SQL Editor에서 SQL 전체 실행
4. 실행 후 확인: `SELECT column_name FROM information_schema.columns WHERE table_name = 'assets' AND column_name = 'user_id'`

코드 검증:
- `npx tsc --noEmit` — 컴파일 에러 0건
- `grep -rn "upsertHoldings(" app/ lib/` — 모든 호출에 두 번째 인수(userId) 포함 확인
- `grep -rn "writePortfolioSnapshot" app/` — userId 인수 포함 확인
- `grep -rn "listGoals()" app/` — 인수 없는 호출 0건 확인 (모두 listGoals(user.id))
- `grep -rn "getAllSnapshots()" app/` — 인수 없는 호출 0건 확인
- `grep -rn "getAssetsWithHoldings()" lib/ app/` — 인수 없는 호출 0건 확인
</verification>

<success_criteria>
1. 6개 사용자 데이터 테이블(assets, transactions, manual_valuations, holdings, goals, portfolio_snapshots)에 user_id UUID NOT NULL 컬럼 존재 — Drizzle 스키마 반영
2. db/migrations/0005_add_user_id.sql에 nullable 추가 → 백필 → NOT NULL → FK → UNIQUE 변경 → 인덱스 → RLS 정책 SQL 모두 포함
3. price_cache 테이블은 변경 없음 (전역 공유)
4. 모든 Server Action INSERT에 userId 포함, UPDATE/DELETE where에 userId 필터 포함
5. db/queries/* 함수 모두 userId 파라미터 받고 where 필터 적용
6. app/(app)/**/page.tsx 모두 supabase.auth.getUser()로 userId 추출 후 쿼리에 전달
7. CRON_TARGET_USER_ID 환경변수 미설정 시 cron이 500 반환 (안전한 실패)
8. npx tsc --noEmit 컴파일 에러 0건
9. .env.local.example에 CRON_TARGET_USER_ID 문서화
</success_criteria>

<output>
실행 완료 후 `.planning/phases/01-foundation/01-04-SUMMARY.md`를 생성한다.
Summary에 포함:
- 수정된 파일 목록 (스키마 6개 + 쿼리 5개 + actions 4개 + pages 5개 + lib 3개 + cron 1개)
- 마이그레이션 SQL 파일 경로 및 수동 실행 방법
- CRON_TARGET_USER_ID 설정 안내
- TypeScript 컴파일 상태
</output>
```

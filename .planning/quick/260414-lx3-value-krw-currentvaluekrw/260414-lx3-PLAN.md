---
phase: quick-260414-lx3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/portfolio/portfolio.ts
  - components/app/overview-tab.tsx
  - db/migrations/0003_fund_valuation_nav_per_unit.sql
autonomous: true
requirements: [QUICK-LX3]

must_haves:
  truths:
    - "펀드 자산의 currentValueKrw = (totalQuantity / 1e8) × latestManualValuationKrw (기준가)"
    - "FundValuationForm 저장 시 value_krw = pricePerUnit 그대로 (곱셈 없음)"
    - "OverviewTab 보유 현황의 현재 평가금액 = (수량 × 기준가)로 표시"
    - "가치 이력 테이블 헤더가 '기준가 (₩/좌)'로 표시됨"
    - "기존 총값 레코드가 마이그레이션 SQL로 단가 역산 신규 레코드 삽입됨"
  artifacts:
    - path: "lib/portfolio/portfolio.ts"
      provides: "computeAssetPerformance — fund 분기 수정"
    - path: "components/app/overview-tab.tsx"
      provides: "FundValuationForm handleSubmit + 이력 테이블 레이블 + 평가금액 표시"
    - path: "db/migrations/0003_fund_valuation_nav_per_unit.sql"
      provides: "기존 fund valuation 총값→단가 변환 레코드 삽입"
  key_links:
    - from: "components/app/overview-tab.tsx (FundValuationForm)"
      to: "manual_valuations.value_krw"
      via: "createManualValuation with raw pricePerUnit"
    - from: "lib/portfolio/portfolio.ts (computeAssetPerformance)"
      to: "currentValueKrw"
      via: "(totalQuantity / 1e8) × latestManualValuationKrw"
---

<objective>
펀드 자산의 manual_valuations.value_krw 의미를 총 평가금액에서 기준가(NAV per unit)로 변경한다.

Purpose: 보유 수량이 변해도 기준가 레코드를 재사용할 수 있고, currentValueKrw 계산이 도메인 원칙에 맞게 수량×단가 구조를 따른다.
Output: portfolio.ts 계산 수정 + FundValuationForm 저장 로직 수정 + OverviewTab 표시 수정 + 기존 데이터 정정 마이그레이션 SQL
</objective>

<execution_context>
@/Users/john/dev/jackpot-pb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/john/dev/jackpot-pb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/john/dev/jackpot-pb/lib/portfolio/portfolio.ts
@/Users/john/dev/jackpot-pb/components/app/overview-tab.tsx
@/Users/john/dev/jackpot-pb/db/queries/assets-with-holdings.ts
@/Users/john/dev/jackpot-pb/db/migrations/0000_sparkling_echo.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: portfolio.ts — fund 분기를 기준가 기반으로 수정</name>
  <files>lib/portfolio/portfolio.ts</files>
  <action>
`computeAssetPerformance` 함수에서 fund 자산 처리 방식을 아래와 같이 변경한다.

현재 코드 (총값 저장 방식):
```
const currentValueKrw = usesManualValuation
  ? (latestManualValuationKrw ?? 0)          // fund: 총값 그대로
  : Math.round((holding.totalQuantity / 1e8) * currentPriceKrw)

const fundUnitPrice =
  holding.assetType === 'fund' && latestManualValuationKrw !== null && holding.totalQuantity > 0
    ? Math.round(latestManualValuationKrw / (holding.totalQuantity / 1e8))   // 역산
    : null
```

변경 후 (기준가 저장 방식):
- fund 자산은 `latestManualValuationKrw`가 기준가(NAV per unit)이므로:
  - `currentValueKrw` = `Math.round((holding.totalQuantity / 1e8) * latestManualValuationKrw)`
  - `currentPriceKrw` = `latestManualValuationKrw` (직접, 역산 불필요)
- 기타 manual(priceType==='manual', fund 아님) 자산은 기존과 동일: `currentValueKrw = latestManualValuationKrw`

구체적 변경:
1. `usesManualValuation` 분기를 fund vs non-fund manual로 세분화:
   ```typescript
   const isFund = holding.assetType === 'fund'
   const isOtherManual = !isFund && holding.priceType === 'manual'
   const usesManualValuation = isFund || isOtherManual
   ```
2. `currentValueKrw` 계산:
   ```typescript
   const currentValueKrw = isFund
     ? Math.round((holding.totalQuantity / 1e8) * (latestManualValuationKrw ?? 0))
     : isOtherManual
     ? (latestManualValuationKrw ?? 0)
     : Math.round((holding.totalQuantity / 1e8) * currentPriceKrw)
   ```
3. `fundUnitPrice` 역산 블록 제거. return 문의 `currentPriceKrw`:
   ```typescript
   currentPriceKrw:
     isFund && latestManualValuationKrw !== null
       ? latestManualValuationKrw
       : isOtherManual && latestManualValuationKrw !== null
       ? latestManualValuationKrw
       : currentPriceKrw,
   ```
   (실제로 fund와 otherManual 모두 같은 값이므로 `usesManualValuation && latestManualValuationKrw !== null ? latestManualValuationKrw : currentPriceKrw`로 단순화 가능)

D-16 주석을 업데이트: "fund: currentValueKrw = (qty/1e8) × latestManualValuationKrw (기준가)"
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <done>타입 오류 없음. fund 자산에서 currentValueKrw = qty × 기준가, currentPriceKrw = 기준가.</done>
</task>

<task type="auto">
  <name>Task 2: FundValuationForm — 저장 로직 및 OverviewTab 표시 수정</name>
  <files>components/app/overview-tab.tsx</files>
  <action>
**A. FundValuationForm.handleSubmit 수정 (저장 로직)**

현재:
```typescript
const totalValueKrw = Math.round(price * (holding.totalQuantity / 1e8))
const result = await createManualValuation(asset.id, {
  valueKrw: totalValueKrw.toString(),
  ...
})
```

변경 후 — 곱셈 없이 pricePerUnit을 그대로 저장:
```typescript
const result = await createManualValuation(asset.id, {
  valueKrw: Math.round(price).toString(),   // 기준가 그대로 (totalValueKrw 계산 제거)
  currency: 'KRW',
  valuedAt: data.valuedAt,
  notes: data.notes ?? null,
})
```
`totalValueKrw` 변수 선언 줄 제거.

**B. OverviewTab — 현재 평가금액 표시 수정**

`latestValuationKrw`는 이제 기준가이므로, 화면에 표시할 평가금액(`displayValueKrw`)을 별도 계산:

현재 코드 (latestValuationKrw를 총값으로 바로 표시):
```tsx
const latestValuationKrw = valuations[0]?.valueKrw ?? null
...
evalProfitKrw = latestValuationKrw - holding!.totalCostKrw
```

변경 후:
```typescript
const latestValuationKrw = valuations[0]?.valueKrw ?? null   // 기준가 (or non-fund 총값)

// fund: currentValueKrw = qty × 기준가; others: latestValuationKrw 그대로
const isFundAsset = asset.assetType === 'fund'
const displayValueKrw =
  isFundAsset && latestValuationKrw !== null && holding !== null
    ? Math.round((holding.totalQuantity / 1e8) * latestValuationKrw)
    : latestValuationKrw

// evalProfitKrw, returnRate 계산에 displayValueKrw 사용
if (hasPosition && displayValueKrw !== null && holding!.totalCostKrw > 0) {
  evalProfitKrw = displayValueKrw - holding!.totalCostKrw
  returnRate = (evalProfitKrw / holding!.totalCostKrw) * 100
}
```

JSX에서 `latestValuationKrw` 표시 부분을 `displayValueKrw`로 교체:
```tsx
{displayValueKrw !== null && (
  <>
    <span className="text-sm text-muted-foreground">현재 평가금액</span>
    <span className="text-sm">₩{formatKrw(displayValueKrw)}</span>
  </>
)}
```

**C. 가치 이력 테이블 헤더 변경**

fund 자산의 이력 테이블에서 컬럼 헤더를 "평가금액 (₩)" → "기준가 (₩/좌)"로 변경.

현재 테이블 헤더 (모든 manual 공통):
```tsx
<TableHead>평가금액 (₩)</TableHead>
```

변경 후:
```tsx
<TableHead>{isFundAsset ? '기준가 (₩/좌)' : '평가금액 (₩)'}</TableHead>
```
`isFundAsset` 변수를 OverviewTab 스코프에서 선언 (asset.assetType === 'fund').
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <done>
    타입 오류 없음.
    FundValuationForm 저장 시 value_krw = pricePerUnit (곱셈 없음).
    OverviewTab 현재 평가금액 = qty × 기준가.
    펀드 이력 테이블 헤더 = "기준가 (₩/좌)".
  </done>
</task>

<task type="auto">
  <name>Task 3: 기존 fund valuation 데이터 단가 역산 마이그레이션 SQL</name>
  <files>db/migrations/0003_fund_valuation_nav_per_unit.sql</files>
  <action>
기존 fund 자산의 manual_valuations 레코드는 총 평가금액으로 저장되어 있다.
append-only 원칙(D-06)에 따라 기존 레코드를 UPDATE하지 않고, 단가로 역산한 새 레코드를 INSERT한다.

마이그레이션 SQL:

```sql
-- 0003_fund_valuation_nav_per_unit.sql
-- Fund valuation semantic change: value_krw was total value, now NAV per unit.
-- For each fund asset, insert a new corrected valuation record
-- that replaces the old total-value record with NAV = total_value / quantity.
-- Old records are left intact (append-only constraint, D-06).
-- The new records are dated the same as the original but with a later created_at
-- so ORDER BY valued_at DESC, created_at DESC will pick the new row as latest.

INSERT INTO manual_valuations (id, asset_id, value_krw, currency, exchange_rate_at_time, valued_at, notes, created_at)
SELECT
  gen_random_uuid(),
  mv.asset_id,
  -- NAV per unit = total_value_krw / (total_quantity / 1e8)
  ROUND(mv.value_krw::numeric / (h.total_quantity::numeric / 1e8))::bigint AS value_krw,
  mv.currency,
  mv.exchange_rate_at_time,
  mv.valued_at,
  COALESCE(mv.notes || ' [마이그레이션: 총값→단가]', '[마이그레이션: 총값→단가]') AS notes,
  NOW() AS created_at
FROM manual_valuations mv
JOIN assets a ON a.id = mv.asset_id
JOIN holdings h ON h.asset_id = mv.asset_id
WHERE a.asset_type = 'fund'
  AND h.total_quantity > 0
  -- Only migrate the latest record per asset (the one currently used for valuation)
  AND mv.id = (
    SELECT id FROM manual_valuations mv2
    WHERE mv2.asset_id = mv.asset_id
    ORDER BY mv2.valued_at DESC, mv2.created_at DESC
    LIMIT 1
  );
```

이 마이그레이션은 자동 실행되지 않는다. 실행 방법:
- `psql $DATABASE_URL -f db/migrations/0003_fund_valuation_nav_per_unit.sql`
또는 Drizzle이 아닌 수동 실행 (스키마 변경 없으므로 `drizzle-kit migrate`와 무관).

주의: holdings.total_quantity = 0인 fund 자산은 마이그레이션 대상에서 제외된다 (WHERE h.total_quantity > 0). 해당 자산은 평가금액이 0이므로 영향 없음.
  </action>
  <verify>cat /Users/john/dev/jackpot-pb/db/migrations/0003_fund_valuation_nav_per_unit.sql | grep -c "INSERT INTO"</verify>
  <done>SQL 파일 존재. INSERT INTO 구문 1개 포함. psql로 실행 시 오류 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form input → DB | pricePerUnit은 사용자 입력 — NaN/음수 차단 필요 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-lx3-01 | Tampering | FundValuationForm pricePerUnit | mitigate | zodResolver의 refine으로 NaN·음수 차단, Math.round로 정수화 후 저장 — 기존 검증 그대로 유지 |
| T-lx3-02 | Denial of Service | migration SQL division | accept | holdings.total_quantity = 0 WHERE 조건으로 divide-by-zero 방지 |
</threat_model>

<verification>
1. `npx tsc --noEmit` — 타입 오류 없음
2. 브라우저에서 fund 자산 상세 페이지 → "현재 가치 업데이트" → 단가(예: 1250) 입력 → 저장
3. 가치 이력 테이블에 1250 표시, 헤더 "기준가 (₩/좌)"
4. 보유 현황 → 현재 평가금액 = 수량 × 1250 (예: 100좌 → ₩125,000)
5. 대시보드/성과 페이지의 fund currentValueKrw = 수량 × 기준가로 계산됨
</verification>

<success_criteria>
- fund 자산: value_krw = 기준가(NAV per unit)로 저장됨
- computeAssetPerformance: fund currentValueKrw = (qty/1e8) × latestManualValuationKrw
- OverviewTab: 펀드 평가금액 표시 = qty × 기준가; 이력 테이블 헤더 "기준가 (₩/좌)"
- 기존 데이터 정정 SQL 파일 존재 (선택적 실행)
- TypeScript 컴파일 오류 없음
</success_criteria>

<output>
After completion, create `.planning/quick/260414-lx3-value-krw-currentvaluekrw/260414-lx3-SUMMARY.md`
</output>
